name: Firmware prerelease
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  build:
    uses: ./.github/workflows/build.yml
# b5 publication is explicit after browser/Android/hardware acceptance.
# Checkpoint tags must not trigger uploads of legacy desktop packages.
