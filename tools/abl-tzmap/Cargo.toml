[package]
name = "abl_tzmap"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
license = "MIT OR Apache-2.0"
description = "ABL-derived TrustZone interface map derivation utility"

[lib]
name = "abl_tzmap"
path = "src/lib.rs"

[[bin]]
name = "abl_tzmap"
path = "src/main.rs"

[dependencies]
clap = { version = "4.5", features = ["derive"] }
sha2 = "0.10"
thiserror = "2"

[dev-dependencies]
tempfile = "3"
