Canoe Toolkit for Linux

Desktop GUI
Run canoe-boot-manager.sh from this directory to open the desktop GUI. The GUI requires the webkit2gtk-4.1, javascriptcoregtk-4.1, and libsoup-3.0 runtime libraries to be installed on the system; it will not start without them. The CLI (canoe) has no such dependency.
