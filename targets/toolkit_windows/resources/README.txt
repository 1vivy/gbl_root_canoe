Canoe Toolkit for Windows

Desktop GUI
Run canoe-boot-manager.bat from this directory to open the desktop GUI. The GUI requires the Microsoft WebView2 runtime; it is present by default on current Windows 11 and can be installed separately on other Windows systems. The GUI will not start without it. The CLI (canoe.exe) has no such dependency.
